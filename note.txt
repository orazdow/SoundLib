replace switch independent etc with master context
then in ctors, if context != nullptr, 
externul func sig_connect(ctx, this), Ctl_connect ...
// will need check for above line, use type enum...
and just use set_context()

then redo connec funcs..

...
-try context
-implement overloads
-cleanup connect funcs in template classes
-maybe use seperate file of overloaded connect functions